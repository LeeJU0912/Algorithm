#include <bits/stdc++.h>
#define FastIO ios_base::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
using namespace std;

int main() {
    FastIO

    int a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;

    cout << d - c << ' ' << e / b << ' ' << f - a;

    return 0;
}