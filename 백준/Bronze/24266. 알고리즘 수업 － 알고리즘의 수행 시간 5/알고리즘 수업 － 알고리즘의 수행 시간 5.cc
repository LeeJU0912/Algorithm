#include <bits/stdc++.h>
#define FastIO ios_base::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
using namespace std;

long long n;

int main() {
    FastIO

    cin >> n;

    cout << n * n * n << '\n' << 3;

    return 0;
}